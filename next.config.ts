import type { NextConfig } from 'next';

// Minimal config: this app needs nothing beyond App Router defaults. Task 11
// is the first task that renders a page, so this file's only job is to let
// `next dev` / `next build` find a config module at all.
const nextConfig: NextConfig = {};

export default nextConfig;
